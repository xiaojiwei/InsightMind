# Fast deployment profile for AD.
#
# This intentionally installs only the core runtime needed to start the
# FastAPI UI, load/build knowledge graphs, use Ad-Hoc/Dashboard APIs, and run
# the default demo. Install optional profiles only when those features are used:
#
#   pip install -r requirements-analysis.txt   # statistical/Insight modeling
#   pip install -r requirements-db-extra.txt   # SQL Server/Oracle/MySQL protocol
#   pip install -r requirements-dev.txt        # pytest and local checks
#   pip install -r requirements-full.txt       # all AD dependencies

-r requirements-core.txt
